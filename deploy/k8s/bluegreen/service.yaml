apiVersion: v1
kind: Service
metadata:
  name: education-platform-svc
  namespace: education-platform
  labels:
    app: education-platform
spec:
  type: ClusterIP
  selector:
    app: education-platform
    version: blue
  ports:
    - name: http
      port: 80
      targetPort: 3000
